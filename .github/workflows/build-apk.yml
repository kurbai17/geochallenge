name: Build APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17
      
      - name: Install Android SDK 34
        run: yes | ${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager

      - name: Create app.json
        run: |
          echo '{
            "name": "geochallenge",
            "displayName": "GeoChallenge",
            "android": {
              "packageName": "com.kurbai.geochallenge"
            }
          }' > app.json

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Verificar config de React Native
        run: npx react-native config

      - name: Make Gradle executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        run: cd android && REACT_NATIVE_ANDROID_PACKAGE_NAME=com.kurbai.geochallenge ./gradlew assembleDebug


      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
